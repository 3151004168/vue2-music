export const songListInfo = (state) => {
  return state.songListInfo;
};
export const songUrl = (state) => {
  return state.songUrl;
};
export const songInfo = (state) => {
  return state.songInfo;
};
export const singerId = (state) => {
  return state.singerId;
};
